import Jquery from 'ember-letter-by-letter/lxl-tween-adapters/jquery';
import { module, test } from 'qunit';

module('Unit | LXLTweenAdapter | jquery');

// Replace this with your real tests.
test('it works', function(assert) {
  const jquery = Jquery.create();
  assert.ok(jquery);
});
