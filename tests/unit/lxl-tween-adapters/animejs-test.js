import Animejs from 'ember-letter-by-letter/lxl-tween-adapters/animejs';
import { module, test } from 'qunit';

module('Unit | LXLTweenAdapter | animejs');

// Replace this with your real tests.
test('it works', function(assert) {
  const animejs = Animejs.create();
  assert.ok(animejs);
});
