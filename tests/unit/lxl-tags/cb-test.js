import Cb from 'ember-letter-by-letter/lxl-tags/cb';
import { module, test } from 'qunit';

module('Unit | LXLTag | cb');

// Replace this with your real tests.
test('it works', function(assert) {
  const cb = Cb.create();
  assert.ok(cb);
});
