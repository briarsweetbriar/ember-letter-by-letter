<h2>Installation</h2>

<p>To install, first:</p>

{{format-markdown '`ember install ember-letter-by-letter;`'}}

<p>Then, in your <code>ember-cli-build.js</code>:</p>

{{format-markdown '```js
var app = new EmberApp(defaults, {
  // Add options here
  babel:{
    includePolyfill: true
  }
});
```'}}
