{{#lxl-container}}
  {{ember-lxl
    text="lxl-tags allow you to change the behavior of <code>ember-letter-by-letter</code> mid-text. You might change the core properties, such as the [[#stagger 10]]<code>stagger</code>[[/stagger]] or [[#tweenEffect (hash x=(hash to=0 from=100))]]<code>tweenEffect</code>[[/tweenEffect]], or do something entirely different, such as pause [[pause 1000]] or clear the text. It's even possible to create your own custom lxl-tags."
  }}
{{/lxl-container}}
